import React from "react";
import PropTypes from "prop-types";

const ProfileBook = ({}) => {
	return (
		<div>
			<h3 className='text-dark'>No History Text</h3>
		</div>
	);
};

ProfileBook.propTypes = {};

export default ProfileBook;
