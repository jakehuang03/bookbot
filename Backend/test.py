import db.crud as crud
import router.endpoints.community as community
import asyncio

# asyncio.run(community.get_ques_by_ques(1))
# print(crud.create_comment(1,1,"test2"))
# asyncio.run(community.get_ques_by_page(5))
